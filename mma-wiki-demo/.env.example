# Supabase環境変数テンプレート
# このファイルをコピーして .env.local を作成し、実際の値を記入してください

# Supabaseプロジェクトの公開URL
# 取得先: Supabase Dashboard → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabaseの公開APIキー（anon key）
# 取得先: Supabase Dashboard → Project Settings → API → anon (public) key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabaseのサービスロールキー（秘密キー）
# ⚠️ 重要: このキーは絶対に公開しないでください (リポジトリに提出禁止)
# 取得先: Supabase Dashboard → Project Settings → API → service_role (secret) key
# 用途: サーバーサイド処理（APIルート等）で使用
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
